## Packages
framer-motion | Smooth animations for layout transitions and status changes
date-fns | Formatting timestamps in logs

## Notes
Tailwind config should extend font family to include 'JetBrains Mono' for the terminal logs.
Polling is required for bot status and logs to keep the UI in sync.
